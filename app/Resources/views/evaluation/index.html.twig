{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evaluations list</h1>

    <table>
        <thead>
            <tr>
                <th>Note</th>
                <th>Commentaire</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evaluation in evaluations %}
            <tr>
                <td><a href="{{ path('evaluation_show', { 'id': evaluation.id }) }}">{{ evaluation.note }}</a></td>
                <td>{{ evaluation.commentaire }}</td>
                <td>{{ evaluation.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('evaluation_show', { 'id': evaluation.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('evaluation_edit', { 'id': evaluation.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evaluation_new') }}">Create a new evaluation</a>
        </li>
    </ul>
{% endblock %}
